{
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "dev": "concurrently \"npm:devclient\" \"npm:devserver\"",
    "devclient": "cd client && npx vite --host",
    "devserver": "cd server && npm run dev"
  }
}
